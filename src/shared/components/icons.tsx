import {
	ArrowUp,
	type LucideProps,
	Send,
	Sparkles,
	Square,
} from "lucide-react";

export function SparklesIcon(props: LucideProps) {
	return <Sparkles {...props} />;
}

export function ArrowUpIcon(props: LucideProps) {
	return <ArrowUp {...props} />;
}

export function StopIcon(props: LucideProps) {
	return <Square {...props} />;
}

export function SendIcon(props: LucideProps) {
	return <Send {...props} />;
}
